function solve(inputMatrix) {
    // ne e moe re6enie!
    // let counter = 0;
    // for (let r = 0; r < inputMatrix.length; r++) {
    //     let subArr = inputMatrix[r];
    //     for (let c = 0; c < inputMatrix[r].length; c++) {
    //         let check = subArr[c];
    //         if (r !== inputMatrix.length - 1) {
    //             if (check === subArr[c + 1] || check === inputMatrix[r + 1][c]) {
    //                 counter++;
    //             }
    //         } else {
    //             if (check === subArr[c + 1]) {
    //                 counter++;
    //             }
    //         }
    //     }
    // }
    // console.log(counter);
}

solve([
    ['2', '3', '4', '7', '0'],

    ['4', '0', '5', '3', '4'],

    ['2', '3', '5', '4', '2'],

    ['9', '8', '7', '5', '4']]);