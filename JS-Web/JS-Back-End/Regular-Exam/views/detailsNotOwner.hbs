<!-- Details Page -->
{{#with auction}}
<section id="catalog-section">

    <h1 class="item">
        {{title}}
    </h1>
    <div class="item padded">

        <div class="layout right large">

            <div class="col">
                <img src="{{imageUrl}}" class="img-large">
            </div>

            <div class="content pad-med">

                <p>In category: <strong>{{category}}</strong></p>
                <p>{{description}}</p>

                <div class="align-center">
                    <div>
                        Current price: $<strong>{{price}}</strong>
                    </div>
                    {{#if hasUser}}
                    <!-- If current user can place a bid -->
                    {{#if notAuthor}}

                    {{#if hasBider}}
                    <!-- If current user is the bidder -->
                    <div>
                        You are currently the <strong>{{bidder.firstName}}</strong> for this auction
                    </div>
                    {{else}}
                    <form class="vertical" method="post" action="/bid/{{id}}">
                        <label><span>Bid amount</span><input type="number" name="bid"></label>
                        <input class="action" type="submit" value="Place bid">
                    </form>
                    {{/if}}
                    {{/if}}
                    {{/if}}
                </div>

            </div>
        </div>

        <footer>Listed by {{author.firstName}} {{author.lastName}}</footer>
    </div>

</section>
{{/with}}